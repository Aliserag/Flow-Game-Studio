{
  "name": "yolo-coin-toss",
  "description": "YOLO — Provably Fair Coin Toss Betting Pool on Flow",
  "version": "1.0.0",
  "scripts": {
    "dev": "cd client && npm run dev",
    "install:client": "cd client && npm install",
    "client:typecheck": "cd client && npx tsc --noEmit",
    "test": "echo 'Note: YOLO contract uses revertibleRandom, no unit tests required'"
  }
}
