name: They Come At Night — Tests

on:
  push:
    branches: [main, "claude/they-come-at-night*"]
    paths:
      - "they-come-at-night/**"
      - ".github/workflows/they-come-at-night-tests.yml"
  pull_request:
    paths:
      - "they-come-at-night/**"
      - ".github/workflows/they-come-at-night-tests.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache Godot
        id: godot-cache
        uses: actions/cache@v4
        with:
          path: ~/godot-bin
          key: godot-4.6-stable-linux

      - name: Install Godot 4.6
        if: steps.godot-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/godot-bin
          cd ~/godot-bin
          # Adjust URL when 4.6 stable lands; this is the canonical pattern.
          curl -L -o godot.zip https://github.com/godotengine/godot-builds/releases/download/4.6-stable/Godot_v4.6-stable_linux.x86_64.zip
          unzip godot.zip
          mv Godot_v4.6-stable_linux.x86_64 godot
          chmod +x godot

      - name: Trigger editor scan (populate class cache)
        working-directory: they-come-at-night
        run: |
          # Godot needs to scan the project once so class_name globals resolve
          # when the test runner loads downstream scripts.
          ~/godot-bin/godot --headless --editor --quit || true

      - name: Run test suite
        working-directory: they-come-at-night
        run: |
          # Tests run via the Main.tscn launcher with the --test user arg.
          # --script mode cannot resolve autoload identifiers at compile time,
          # so we boot the project normally and route via cmdline_user_args.
          ~/godot-bin/godot --headless res://scenes/Main.tscn -- --test

      - name: Run smoke (50-turn solo)
        working-directory: they-come-at-night
        run: |
          ~/godot-bin/godot --headless res://scenes/Main.tscn -- --smoke

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-output
          path: they-come-at-night/tests/
          retention-days: 7
